import { Button, Text, View } from "react-native";

export function Connect() {
    return (
        <View>
            <Text>Connect</Text>
            <Button title="Connect" onPress={() => { }} />
        </View>
    )
}
